[project]
name = "binance-alpha-hunter"
version = "0.1.0"
description = "Binance Alpha 场景的 OpenClaw Skill"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
